import React, { FunctionComponent } from 'react';


const RecipeUpdateView: FunctionComponent = (props): JSX.Element => {
    return <React.Fragment>
        Recipe Update Form View
    </React.Fragment>;
};

export default RecipeUpdateView;