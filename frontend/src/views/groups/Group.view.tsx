import React, { FunctionComponent } from 'react';
// styling
import { useStyles } from './group.view.style';

const GroupView: FunctionComponent = (props): JSX.Element => {
    return (
        <React.Fragment>
            Group View goes here
        </React.Fragment>
    );
}

export default GroupView;