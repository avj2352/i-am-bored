import React, { FunctionComponent } from 'react';

export const HomeSkeletonLoading: FunctionComponent = (props):JSX.Element => {
    return (
        <React.Fragment>
            Skeleton Loading Comes here
        </React.Fragment>
    );
};