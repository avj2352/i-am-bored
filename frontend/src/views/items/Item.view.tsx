import React, { FunctionComponent } from 'react';
// styling
import { useStyles } from './item.view.style';

const ItemView: FunctionComponent = (props): JSX.Element => {
    return (
        <React.Fragment>
            Items View goes here
        </React.Fragment>
    );
}

export default ItemView;